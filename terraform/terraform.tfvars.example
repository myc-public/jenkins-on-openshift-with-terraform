# Ce fichier ne documente QUE les variables non sensibles.
# Les secrets vont dans terraform/secrets.auto.tfvars (gitignoré, chargé automatiquement
# par Terraform) : voir secrets.auto.tfvars.example.
# TODO: basculer les secrets vers HashiCorp Vault / AWS Secrets Manager.
#
# Copier ce fichier en terraform.tfvars (gitignoré) si tu veux surcharger les valeurs par défaut.

namespace        = "gregorie769-dev"
sonar_server_url = "https://sonarcloud.io"
smtp_host        = "smtp.gmail.com"
smtp_port        = "465"
apps_domain      = "apps.rm3.7wse.p1.openshiftapps.com"  # domaine des Routes : change avec le cluster (REBUILD.md etape 2)
